import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    h4: {
      fontWeight: 800,
    },
  },
  palette: {
    text: {
      light: '#FFFFFF',
      info: '#2299FF',
      link: '#0055CC',
    },
  },
});
